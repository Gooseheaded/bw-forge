# Legacy Reference Data

The files in this directory are retained as historical scrape/reference material.

- They are not loaded by the replay-analysis runtime.
- They are not the authoritative source for the data embedded in `build-order.html`.
- `fetch_liquipedia.py` exists to support this legacy reference corpus, not the active replay-analysis toolchain.
