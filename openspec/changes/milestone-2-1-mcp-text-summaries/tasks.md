1. Add shared MCP text-formatting helpers for ranked lists, timing stats, examples, and truncation notices.
2. Update discovery tools so `content.text` includes actual names, items, matchups, and unit types instead of counts only.
3. Update aggregate analytics tools so `content.text` includes timing medians, percentages, unit medians, economy medians, and combat summaries.
4. Update older primitive query tools so their text summaries include replay names, event names, timings, sample values, or death counts.
5. Add integration assertions that `content.text` contains meaningful values for representative discovery, timing, composition, economy, death, replay-card, and primitive-tool calls.
6. Re-run TypeScript checks and fixture-backed MCP tests.
